diff --git a/node_modules/@analogjs/vite-plugin-angular/src/lib/angular-vite-plugin.js b/node_modules/@analogjs/vite-plugin-angular/src/lib/angular-vite-plugin.js
index 8a3942f..97efe6f 100644
--- a/node_modules/@analogjs/vite-plugin-angular/src/lib/angular-vite-plugin.js
+++ b/node_modules/@analogjs/vite-plugin-angular/src/lib/angular-vite-plugin.js
@@ -4,7 +4,8 @@ import * as compilerCli from '@angular/compiler-cli';
 import { createRequire } from 'node:module';
 import * as ngCompiler from '@angular/compiler';
 import { globSync } from 'tinyglobby';
-import { defaultClientConditions, normalizePath, preprocessCSS, } from 'vite';
+import { normalizePath, preprocessCSS } from 'vite';
+import * as viteModule from 'vite';
 import { buildOptimizerPlugin } from './angular-build-optimizer-plugin.js';
 import { jitPlugin } from './angular-jit-plugin.js';
 import { createCompilerPlugin } from './compiler-plugin.js';
@@ -33,6 +34,9 @@ export var DiagnosticModes;
  * Ignore .tsx extensions
  */
 const TS_EXT_REGEX = /\.[cm]?(ts)[^x]?\??/;
+const pluginDefaultClientConditions = Array.isArray(viteModule?.defaultClientConditions)
+    ? viteModule.defaultClientConditions
+    : ['module', 'browser', 'import', 'default'];
 const classNames = new Map();
 export function angular(options) {
     /**
@@ -146,7 +150,7 @@ export function angular(options) {
                     resolve: {
                         conditions: [
                             'style',
-                            ...(config.resolve?.conditions || defaultClientConditions),
+                            ...(config.resolve?.conditions || pluginDefaultClientConditions),
                         ],
                     },
                 };
